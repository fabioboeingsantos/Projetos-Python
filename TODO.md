# TODO

- [ ] Criar/atualizar a consulta SQL pedida: **nome da localidade** e **média salarial** de cada localidade que tenha **2 ou mais mulheres**.
- [ ] Validar o uso correto de joins entre `localidade`, `departamento` e `empregado`.
- [ ] Usar `GROUP BY` na localidade e `HAVING COUNT(*) >= 2` considerando `genero_empregado = 'feminino'`.
- [ ] (Opcional) Preparar uma versão em string SQL limpa para copiar e colar.

